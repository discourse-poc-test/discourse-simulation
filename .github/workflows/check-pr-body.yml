name: Check PR Body
on:
  pull_request_target:
    types: [opened, reopened, edited]

jobs:
  sanitize:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Vulnerable Execution
        run: |
          echo "========================================"
          echo "VULNERABILITY EXPLOITED!"
          echo "========================================"
          echo ""
          echo "pull_request_target triggered"
          echo "Running in BASE repository context"
          echo "GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN != '' }}"
          echo "Repository: ${{ github.repository }}"
          echo ""
          echo "Attacker fork SHA executing with BASE privileges"
          echo "========================================"
